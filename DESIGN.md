# 草叢突擊 Bushwhack — 數值與玩法設計

本文件對應企劃 S1 / N2；實際平衡參數唯一來源為 `game.js` 頂端的 `CONFIG`。調整數值時以程式設定為準，並同步更新本文件。所有數值均為單人、每秒 60 幀目標下的設計值。

## 玩家與武器（M1、M2、M7、M9）

| 項目 | 初值 | 規則 |
| --- | ---: | --- |
| HP | 100 | 歸零立即結算；受傷閃紅、粒子與 0.65 秒無敵時間 |
| 移動速度 | 245 px/s | 對角線移動正規化；涉水乘 0.5 |
| 隱匿 | 草叢內且未暴露 | 草叢內射擊暴露 2.4 秒；離開立即暴露；敵人立即失去隱匿目標 |
| 拾取距離 | 40 px | 走近掉落物自動拾取；掉落物 30 秒後消失 |
| 基礎武器 | 傷害 18、3.3 發/s、單發、射程 480 px | 子彈速度 800 px/s；牆壁阻擋，水潭不阻擋 |

| 強化（最高 5 級） | 每級效果 | 首級成本（金幣／零件） | 每級增加成本（金幣／零件） |
| --- | --- | ---: | ---: |
| 高能彈頭 | 傷害 +6 | 12 / 1 | +10 / +1 |
| 急速槍機 | 每秒發射 +0.65 發 | 14 / 1 | +11 / +1 |
| 多重槍管 | 每次 +1 顆散射彈（最多額外 4 顆） | 18 / 2 | +15 / +1 |
| 長程瞄具 | 射程 +90 px | 11 / 1 | +9 / +1 |

每顆散射彈造成完整傷害，相鄰彈道角差 0.14 弧度。升級立刻修改子彈、開火頻率或射程；多重槍管改變彈幕外觀。`B` 或側欄按鈕可隨時打開暫停中的強化選單。HUD 顯示 HP、波次、擊倒數、金幣、零件與四項武器數值。

## NPC 行為與難度（M3、M5、M8）

| 類型 | 基礎 HP | 移速 | 偵測距離 | 攻擊距離 | 基礎傷害 | 攻擊間隔 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 近戰追獵者 | 42 | 94 px/s | 305 px | 30 px | 9 | 1.05 s |
| 遠程哨兵（第 2 波起） | 32 | 76 px/s | 365 px | 270 px | 8 | 1.9 s |

狀態流：`wander`（每 1.8–3.6 秒變更方向）→ `alert`（發現可見玩家，0.28 秒反應）→ `chase`（繞開牆壁接近）→ `attack`（近戰接觸／遠程直線彈）。牆壁遮蔽偵測；水潭不遮蔽。失去視線 2.5 秒後返回游蕩；玩家進草叢時立即放棄追擊和攻擊。敵人進草叢時，距離玩家超過 110 px 僅以淡影顯示。

每波敵人數 `min(30, 2 + 2 × 波次)`；第 2 波開始出現遠程敵人，其機率從 26% 隨波次每次提高 2.5%，最高 62%。敵人 HP 乘 `1 + 0.14 × (波次 - 1)`；傷害乘 `1 + 0.095 × (波次 - 1)`，取整數。全部擊倒後 3.5 秒開始下一波，沒有波數上限。波次期間敵人每 0.72 秒補入一名，出生位置距玩家至少 330 px。

## 寶箱與掉落表（M4、M7）

| 來源 | 必掉金幣 | 零件 | 額外獎勵 |
| --- | --- | --- | --- |
| 每名 NPC | 3–6 | 38% 機率掉 1 個 | 無 |
| 每個補給箱 | 8–13 | 必掉 1–2 個 | 28% 機率掉 18 HP 治療包 |

寶箱 HP 為 `23 + 3 × (波次 - 1)`；每場初始 3 個，少於 2 個時每 5 秒補 1 個，至場上至少 2 個（設計範圍 2–4 個）。每次擊破必須產生金幣與零件。寶箱位於可通行、非草叢／非水潭／非牆壁的位置，距玩家至少 130 px。掉落需要走近拾取，不會自動入帳。

## 地形與地圖（M5、M6、G15）

單張 1800 × 1200 px 地圖；**每次開始或重新出擊均重新隨機生成** 11 面牆、7 處水潭、19 片草叢。地形邊緣保留 20 px 間距，不互相重疊，中心出生區淨空 200 px。每加一面牆，以 30 px 格點對玩家碰撞半徑加安全邊距進行 flood fill；只有所有可行走格子仍連通才接受該牆。水潭與草叢不阻擋行走、子彈或視線。牆壁使用軸向滑動碰撞與子彈線段交會判定；子彈高速移動也不能穿牆。

## 操作與勝負（M1、M9）

WASD／方向鍵移動，滑鼠瞄準、按住左鍵射擊，`B` 開關強化選單、`Esc` 關閉；HP 歸零出現抵達波次、擊倒數及累計金幣結算。重新出擊重置所有本局進度並生成另一張地圖。沒有後端、帳號、存檔或必需的外部素材。
